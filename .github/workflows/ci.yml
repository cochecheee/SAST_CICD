#name: CI Workflow
#
#on:
#  push:
#    branches: [main]
#  pull_request:
#    branches: [main]
#
#jobs:
#  build:
#    name: Build Application
#    runs-on: ubuntu-latest
#    permissions:
#      security-events: write
#      actions: read
#      contents: read
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: 'gradle'
#
#      - name: Grant execute permission
#        run: chmod +x gradlew
#
#      - name: Build application
##        command to build the project (changing command)
#        run: ./gradlew clean build -x test --no-daemon
#
#      - name: Upload build output
#        uses: actions/upload-artifact@v4
#        with:
#          name: build-output
#          path: build/classes/java/main
#          retention-days: 1
#
#  codeql:
#    name: CodeQL
#    needs: build
#    runs-on: ubuntu-latest
#    permissions:
#      actions: read
#      contents: read
#      security-events: write
#
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: 'gradle'
#
#      - name: Grant execute permission
#        run: chmod +x gradlew
#
#      - name: Initialize CodeQL
#        uses: github/codeql-action/init@v4
#        with:
#          languages: java
#
#      - name: Build with CodeQL tracing
#        run: ./gradlew clean compileJava --no-daemon
#
#      - name: Analyze
#        uses: github/codeql-action/analyze@v4
#        with:
#          category: "/language:java"
#          upload: false
#          output: codeql-results
#
#      - name: Extract JSON from SARIF
#        run: |
#          SARIF_FILE=$(find codeql-results -name "*.sarif" | head -n 1)
#          if [ -z "$SARIF_FILE" ]; then
#            echo "No SARIF file found!"
#            exit 1
#          fi
#          echo "Using SARIF: $SARIF_FILE"
#          # convert SARIF → JSON (only results section)
#          jq '.runs[].results' "$SARIF_FILE" > codeql-results.json
#          echo "Created codeql-results.json"
#
#      - name: Convert CodeQL SARIF to Sonar format
#        run: |
#          # Find the actual SARIF file
#          SARIF_FILE=$(find codeql-results -name "*.sarif" | head -n 1)
#          if [ -z "$SARIF_FILE" ]; then
#            echo "No SARIF file found, creating empty report"
#            echo '{"issues":[]}' > sonar-issues.json
#            exit 0
#          fi
#          echo "Found SARIF file: $SARIF_FILE"
#          if [ -f "scripts/convert_codeql_to_sonar.py" ]; then
#            python scripts/convert_codeql_to_sonar.py \
#              --input "$SARIF_FILE" \
#              --output sonar-issues.json
#          else
#            echo "Converter script not found, creating empty report"
#            echo '{"issues":[]}' > sonar-issues.json
#          fi
#
#      - name: Upload CodeQL JSON Output
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: codeql-json-results-${{ github.run_number }}
#          path: codeql-results.json
#          retention-days: 1
#
#  dependency-check:
#    name: Dependency Check
#    needs: build
#    runs-on: ubuntu-latest
#    permissions:
#      security-events: write
#      actions: read
#      contents: read
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: 'gradle'
#
#      - name: Grant execute permission
#        run: chmod +x gradlew
#
#      - name: Cache CVE database
#        uses: actions/cache@v4
#        with:
#          path: ~/.gradle/dependency-check-data
#          key: depcheck-${{ hashFiles('**/build.gradle') }}
#
#      - name: Run Dependency-Check
#        env:
#          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
#        run: |
#          mkdir -p build/reports
#          ./gradlew dependencyCheckAnalyze \
#            --no-daemon \
#            -Dnvd.api.key="${NVD_API_KEY}" \
#            -Dformats=JSON,HTML,XML
#
#      - name: Upload reports
#        uses: actions/upload-artifact@v4
#        with:
#          name: dependency-check-reports
#          path: build/reports/dependency-check-report.*
#          retention-days: 1
#
#  sonarqube:
#    name: SonarQube
#    needs: build
#    runs-on: ubuntu-latest
#    permissions:
#      security-events: write
#      actions: read
#      contents: read
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Download pre-built bytecode
#        uses: actions/download-artifact@v4
#        with:
#          name: build-output
#          path: build/classes/java/main
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: 'gradle'
#
#      - name: Grant execute permission
#        run: chmod +x gradlew
#
#      - name: Cache Sonar scanner
#        uses: actions/cache@v4
#        with:
#          path: ~/.sonar/cache
#          key: sonar-${{ runner.os }}-${{ hashFiles('**/build.gradle') }}
#
#      - name: SonarQube Scan
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
#        run: |
#          ./gradlew sonar \
#            --no-daemon \
#            -Dsonar.projectKey=sast-social \
#            -Dsonar.host.url=${SONAR_HOST_URL} \
#            -Dsonar.token=${SONAR_TOKEN}
#        continue-on-error: true
#
#  docker:
#    name: Docker Image Scan
#    needs: [codeql, dependency-check, sonarqube]
#    runs-on: ubuntu-latest
#    permissions:
#      security-events: write
#      actions: read
#      contents: read
#    steps:
#      - name: Checkout repository
#        uses: actions/checkout@v4
#        with:
#          fetch-depth: 0
#
#      - name: Setup JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#          cache: 'gradle'
#
#      - name: Grant execute permission
#        run: chmod +x gradlew
#
#      - name: Build Docker image
#        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest .
#
#      - name: Trivy scan
#        uses: aquasecurity/trivy-action@master
#        with:
#          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
#          format: 'sarif'
#          output: 'trivy-results.sarif'
#          exit-code: '0'
#          ignore-unfixed: true
#          vuln-type: 'os,library'
#          severity: 'CRITICAL,HIGH'
#        continue-on-error: true
#
#      - name: Generate Trivy JSON Report
#        if: always()
#        uses: aquasecurity/trivy-action@master
#        with:
#          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
#          format: 'json'
#          output: 'trivy-results.json'
#          exit-code: '0'
#          ignore-unfixed: true
#          vuln-type: 'os,library'
#          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
#        continue-on-error: true
#
#      - name: Generate Trivy Table Report
#        if: always()
#        uses: aquasecurity/trivy-action@master
#        with:
#          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
#          format: 'table'
#          output: 'trivy-results.txt'
#          exit-code: '0'
#          ignore-unfixed: true
#          vuln-type: 'os,library'
#          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
#        continue-on-error: true
#
#      - name: Upload Trivy Scan Results
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: trivy-scan-results-${{ github.run_number }}
#          path: |
#            trivy-results.sarif
#            trivy-results.json
#            trivy-results.txt
#          retention-days: 1
#
#      - name: Login & Push to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - run: docker push ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest
#
#  report:
#    name: Final Report
#    needs: [docker]
#    runs-on: ubuntu-latest
#    if: always()
#    steps:
#      - name: Upload All Security Reports (Combined)
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: all-security-reports-${{ github.run_number }}
#          path: |
#            codeql-results.sarif
#            codeql-results/
#            build/reports/dependency-check-report.*
#            sonar-issues.json
#            build/sonar/report-task.txt
#            trivy-results.sarif
#            trivy-results.json
#            trivy-results.txt
#          retention-days: 30
#
#      - name: Security Scan Summary
#        if: always()
#        run: |
#          echo "================================================"
#          echo "SECURITY SCAN SUMMARY"
#          echo "================================================"
#          if [ "${{ steps.codeql_check.outputs.codeql_failed }}" == "true" ]; then
#            echo "CodeQL: FAILED (Critical issues found)"
#          else
#            echo "CodeQL: PASSED"
#          fi
#          if [ "${{ steps.sonar_verify.outputs.sonar_unavailable }}" == "true" ]; then
#            echo "SonarQube: SKIPPED (Unavailable)"
#          elif [ "${{ steps.quality_gate.outcome }}" == "failure" ]; then
#            echo "SonarQube Quality Gate: FAILED"
#          else
#            echo "SonarQube: PASSED"
#          fi
#          if [ "${{ steps.trivy_scan.outcome }}" == "failure" ]; then
#            echo "Trivy Scan: FAILED (Vulnerabilities found)"
#          else
#            echo "Trivy Scan: PASSED"
#          fi
#          echo "================================================"
#          echo "All artifacts have been uploaded"
#          echo "================================================"
name: CI Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write
  actions: read

# ==============================================================================
# PIPELINE FLOW:
#   build
#     └─> [codeql | semgrep | spotbugs | gitleaks | dependency-check | trivy-fs]  (parallel)
#               └─> sonarqube  (aggregate + import all reports)
#                       └─> docker
# ==============================================================================

jobs:

  # ============================================================
  # STAGE 1: BUILD
  # Compile project, upload bytecode for downstream jobs
  # ============================================================
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Compile project (skip tests for speed)
        run: ./gradlew clean compileJava --no-daemon

      - name: Upload compiled classes
        uses: actions/upload-artifact@v4
        with:
          name: build-classes
          path: build/classes/java/main
          retention-days: 1

  # ============================================================
  # STAGE 2: SAST SCANS (ALL RUN IN PARALLEL)
  # ============================================================

  # --- 2a. SpotBugs (bytecode-level bug analysis) ---
  spotbugs:
    name: SpotBugs
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Download compiled classes
        uses: actions/download-artifact@v4
        with:
          name: build-classes
          path: build/classes/java/main

      - name: Grant execute permission
        run: chmod +x gradlew

      # SpotBugs must output SARIF for SonarQube to import.
      # Requires com.github.spotbugs plugin >= 5.x and sarif output format configured in build.gradle:
      #   spotbugs { reportLevel = 'high'; reports { sarif.enabled = true; xml.enabled = false } }
      - name: Run SpotBugs
        run: ./gradlew spotbugsMain --no-daemon
        continue-on-error: true

      - name: Upload SpotBugs SARIF
        uses: actions/upload-artifact@v4
        with:
          name: spotbugs-report
          path: build/reports/spotbugs/main.sarif
          retention-days: 1

  # --- 2b. Gitleaks (secret scanning) ---
  gitleaks:
    name: Gitleaks Secret Scan
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

  # --- 2c. Semgrep (SAST, fast pattern-based) ---
  semgrep:
    name: Semgrep
    runs-on: ubuntu-latest
    needs: build
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@v4

      - name: Run Semgrep (SARIF output)
        run: semgrep scan --config=auto --sarif --output=semgrep.sarif
        continue-on-error: true

      - name: Upload Semgrep SARIF
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: semgrep.sarif
          retention-days: 1

  # --- 2d. CodeQL (deep semantic analysis) ---
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    needs: build
    permissions:
      security-events: write
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      # Autobuild recompiles — needed for CodeQL tracing
      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java"
          # 'upload: false' keeps the SARIF local so we can pass it to Sonar
          upload: false
          output: codeql-results
        continue-on-error: true

      - name: Upload CodeQL SARIF
        uses: actions/upload-artifact@v4
        with:
          name: codeql-report
          path: codeql-results/java.sarif
          retention-days: 1

  # --- 2e. OWASP Dependency-Check (SCA) ---
  dependency-check:
    name: Dependency Check
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Cache NVD CVE database
        uses: actions/cache@v4
        with:
          path: ~/.gradle/dependency-check-data
          key: depcheck-${{ hashFiles('**/build.gradle') }}
          restore-keys: depcheck-

      - name: Run OWASP Dependency-Check
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: |
          ./gradlew dependencyCheckAnalyze --no-daemon \
            -Dnvd.api.key="${NVD_API_KEY}" \
            -Dformats=JSON,HTML
        continue-on-error: true

      - name: Upload Dependency-Check JSON report
        uses: actions/upload-artifact@v4
        with:
          name: dep-check-report
          # SonarQube dependency-check plugin reads the JSON report
          path: build/reports/dependency-check-report.json
          retention-days: 1

  # --- 2f. Trivy filesystem scan ---
  trivy-fs:
    name: Trivy FS Scan
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      # Scanning the filesystem (not image) gives file-level location data
      # that SonarQube can map back to source lines.
      - name: Run Trivy FS scan (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-fs.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
        continue-on-error: true

      - name: Upload Trivy SARIF
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-fs.sarif
          retention-days: 1

  # --- 2g. ESLint + security plugin (frontend JS) ---
  eslint:
    name: ESLint Security
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install ESLint + security plugin
        run: |
          npm install --save-dev \
            eslint \
            eslint-plugin-security \
            @microsoft/eslint-formatter-sarif

      - name: Create ESLint config
        run: |
          cat > .eslintrc.json << 'EOF'
          {
            "plugins": ["security"],
            "extends": ["plugin:security/recommended"],
            "env": { "browser": true, "es2021": true },
            "rules": {
              "security/detect-eval-with-expression": "error",
              "security/detect-non-literal-regexp": "warn",
              "security/detect-object-injection": "warn",
              "security/detect-unsafe-regex": "error"
            }
          }
          EOF

      - name: Run ESLint (SARIF output)
        # Only scan JS files; allow failure so pipeline continues
        run: |
          JS_FILES=$(find src/main/resources/static -name "*.js" 2>/dev/null | head -50)
          if [ -z "$JS_FILES" ]; then
            echo "No JS files found, creating empty SARIF"
            echo '{"version":"2.1.0","runs":[{"tool":{"driver":{"name":"ESLint","rules":[]}},"results":[]}]}' > eslint-results.sarif
          else
            npx eslint $JS_FILES \
              --format @microsoft/eslint-formatter-sarif \
              --output-file eslint-results.sarif || true
          fi
        continue-on-error: true

      - name: Upload ESLint SARIF
        uses: actions/upload-artifact@v4
        with:
          name: eslint-report
          path: eslint-results.sarif
          retention-days: 1

  # ============================================================
  # STAGE 3: SONARQUBE — AGGREGATE & IMPORT
  # Collects every report artifact, then runs a single sonar scan
  # that imports them all into the dashboard.
  #
  # Required SonarQube plugins (install on server):
  #   - sonar-dependency-check-plugin
  #   - sonar-sarif-plugin (or built-in SARIF support >= SQ 9.x)
  # ============================================================
  sonarqube:
    name: SonarQube Analysis
    runs-on: ubuntu-latest
    needs: [spotbugs, semgrep, codeql, dependency-check, trivy-fs, eslint]
    # Run even if some scan jobs fail so partial data still reaches Sonar
    if: always()
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ---- Download all artifacts ----
      - name: Download compiled classes
        uses: actions/download-artifact@v4
        with:
          name: build-classes
          path: build/classes/java/main

      - name: Download SpotBugs report
        uses: actions/download-artifact@v4
        with:
          name: spotbugs-report
          path: reports/spotbugs
        continue-on-error: true

      - name: Download CodeQL report
        uses: actions/download-artifact@v4
        with:
          name: codeql-report
          path: reports/codeql
        continue-on-error: true

      - name: Download Semgrep report
        uses: actions/download-artifact@v4
        with:
          name: semgrep-report
          path: reports/semgrep
        continue-on-error: true

      - name: Download Trivy report
        uses: actions/download-artifact@v4
        with:
          name: trivy-report
          path: reports/trivy
        continue-on-error: true

      - name: Download Dependency-Check report
        uses: actions/download-artifact@v4
        with:
          name: dep-check-report
          path: reports/depcheck
        continue-on-error: true

      - name: Download ESLint report
        uses: actions/download-artifact@v4
        with:
          name: eslint-report
          path: reports/eslint
        continue-on-error: true

      # ---- Build list of available SARIF files dynamically ----
      # This prevents Sonar from failing when a scan produced no output.
      - name: Collect available SARIF paths
        id: sarif_paths
        run: |
          SARIF_FILES=""
          for f in \
            reports/spotbugs/main.sarif \
            reports/codeql/java.sarif \
            reports/semgrep/semgrep.sarif \
            reports/trivy/trivy-fs.sarif \
            reports/eslint/eslint-results.sarif; do
            if [ -f "$f" ]; then
              SARIF_FILES="${SARIF_FILES}${f},"
            else
              echo "WARNING: $f not found, skipping"
            fi
          done
          # Remove trailing comma
          SARIF_FILES="${SARIF_FILES%,}"
          echo "paths=$SARIF_FILES" >> "$GITHUB_OUTPUT"
          echo "Collected SARIF paths: $SARIF_FILES"

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Sonar scanner
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: sonar-${{ runner.os }}-${{ hashFiles('**/build.gradle') }}

      - name: Grant execute permission
        run: chmod +x gradlew

      # ---- Import everything into SonarQube ----
      - name: SonarQube Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: |
          ./gradlew sonar --no-daemon \
            -Dsonar.projectKey=sast-social \
            -Dsonar.host.url="${SONAR_HOST_URL}" \
            -Dsonar.token="${SONAR_TOKEN}" \
            -Dsonar.java.binaries=build/classes/java/main \
            -Dsonar.sarifReportPaths="${{ steps.sarif_paths.outputs.paths }}" \
            -Dsonar.dependencyCheck.jsonReportPath=reports/depcheck/dependency-check-report.json

  # ============================================================
  # STAGE 4: DOCKER BUILD & PUSH
  # Only runs after Sonar succeeds
  # ============================================================
  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [sonarqube]
    if: success()
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest .

      - name: Trivy container image scan (SARIF)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
          format: 'sarif'
          output: 'trivy-image.sarif'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true

      - name: Trivy container image scan (JSON — full detail)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
          format: 'json'
          output: 'trivy-image.json'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH,MEDIUM'
        continue-on-error: true

      - name: Upload Trivy image scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-image-scan-${{ github.run_number }}
          path: |
            trivy-image.sarif
            trivy-image.json
          retention-days: 30

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest