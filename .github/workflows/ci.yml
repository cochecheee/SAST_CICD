name: CI Workflow (Gradle)

on:
  push:
    branches:
      - main  # chỉ trigger khi có push (merge) vào main

jobs:
  ci:
    name: Security & Build Pipeline
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # để SonarQube phân tích đầy đủ lịch sử

      # Set up JDK
      - name: Set up JDK 17 for Gradle
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      # Initialize CodeQL
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      # Build project cho CodeQL phân tích
      - name: Build with Gradle
        run: ./gradlew compileJava --no-daemon

      # Perform CodeQL Analysis (chỉ xuất SARIF, không upload lên GitHub)
      - name: Perform CodeQL Analysis (local only)
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:java"
          upload: false
          output: codeql-results

      # Copy SARIF file
      - name: Copy SARIF file
        run: |
          if [ -f "codeql-results/java.sarif" ]; then
            cp codeql-results/java.sarif codeql-results.sarif
            echo "✅ Copied SARIF file to codeql-results.sarif"
          else
            echo "❌ SARIF file not found, searching..."
            find . -name "*.sarif" -type f -exec cp {} codeql-results.sarif \;
          fi

      # Convert CodeQL SARIF to Sonar format
      - name: Convert CodeQL SARIF to Sonar format
        run: |
          python scripts/convert_codeql_to_sonar.py \
            --input codeql-results.sarif \
            --output sonar-issues.json

      # Cache Dependency-Check
      - name: Cache Dependency-Check
        uses: actions/cache@v4
        with:
          path: |
            dependency-check
            ~/OWASP-Dependency-Check/data
          key: ${{ runner.os }}-dependency-check-9.2.0-${{ hashFiles('**/build.gradle', '**/build.gradle.kts') }}
          restore-keys: |
            ${{ runner.os }}-dependency-check-9.2.0-
            ${{ runner.os }}-dependency-check-

      # Install Dependency-Check
      - name: Install Dependency-Check
        run: |
          if [ ! -d "dependency-check" ]; then
            echo "📥 Downloading Dependency-Check v9.2.0 (stable version)..."
            wget -q https://github.com/jeremylong/DependencyCheck/releases/download/v9.2.0/dependency-check-9.2.0-release.zip
            echo "📦 Extracting..."
            unzip -q dependency-check-9.2.0-release.zip
            echo "✅ Installation complete"
          else
            echo "✅ Using cached Dependency-Check"
          fi
          ls -la dependency-check/bin/

      # Run Dependency-Check
      - name: Run Dependency-Check
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: |
          mkdir -p build/reports
          echo "🔍 Running Dependency-Check scan..."
          
          # Build base command
          CMD="./dependency-check/bin/dependency-check.sh"
          CMD="$CMD --scan ."
          CMD="$CMD --out build/reports"
          CMD="$CMD --format XML"
          CMD="$CMD --format JSON"
          CMD="$CMD --format HTML"
          CMD="$CMD --project aloute-spring"
          CMD="$CMD --exclude '**/node_modules/**'"
          CMD="$CMD --exclude '**/test/**'"
          CMD="$CMD --exclude '**/tests/**'"
          
          # Add suppression file if it exists
          if [ -f "dependency-check-suppressions.xml" ]; then
            CMD="$CMD --suppression dependency-check-suppressions.xml"
            echo "✅ Using suppression file"
          fi
          
          # Add NVD API key if available
          if [ -n "$NVD_API_KEY" ]; then
            echo "✅ Using NVD API Key (faster CVE updates)"
            CMD="$CMD --nvdApiKey $NVD_API_KEY"
          else
            echo "⚠️ No NVD API Key found - using slower public feed"
            echo "💡 Get a free API key at: https://nvd.nist.gov/developers/request-an-api-key"
          fi
          
          # Execute command with retry logic
          set +e  # Don't exit on error
          $CMD
          EXIT_CODE=$?
          set -e
          
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ Dependency-Check completed successfully"
          else
            echo "⚠️ Dependency-Check completed with exit code: $EXIT_CODE"
            # Check if reports were generated despite errors
            if [ -f "build/reports/dependency-check-report.xml" ]; then
              echo "✅ Reports were generated, continuing..."
            else
              echo "❌ No reports generated, but continuing pipeline..."
            fi
          fi
          
          echo "📋 Generated reports:"
          ls -lh build/reports/ || echo "No reports directory found"

      # Build & Test
      - name: Build & Test (Gradle)
        run: ./gradlew clean build --no-daemon -x test

      # SonarQube Scan
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=sast-social
            -Dsonar.externalIssuesReportPaths=sonar-issues.json
            -Dsonar.dependencyCheck.xmlReportPath=build/reports/dependency-check-report.xml
            -Dsonar.dependencyCheck.jsonReportPath=build/reports/dependency-check-report.json

      # Check Quality Gate
      - name: Check SonarQube Quality Gate
        uses: sonarsource/sonarqube-quality-gate-action@v1
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          timeout: 300

      # Build Docker image
      - name: Build Docker Image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest .

      # Scan Docker image with Trivy
      - name: Scan Docker Image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL'

      # Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Push Docker image
      - name: Push Docker Image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/aloute-spring:latest

      # Upload Security Reports
      - name: Upload Security Reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            build/reports/dependency-check-report.*
            sonar-issues.json
            codeql-results.sarif
            codeql-results/java.sarif
        if: always()  # Upload even if previous steps fail